# kuro-lean Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Xây CLI `kt` (Bun+TS) giảm token Claude Code bằng cách nén output lệnh shell, đo context, chặn lệnh ngốn token — theo nguyên tắc "nén nhiễu, giữ tín hiệu".

**Architecture:** Một binary `kt` nhiều subcommand. PreToolUse hook rewrite `<cmd>` → `kt run -- <cmd>`; `kt run` spawn lệnh, chọn compressor theo profile, in bản nén + lưu full ra `.kt/runs/`. `kt status` render statusline từ JSON stdin. `kt hook-compress`/`kt hook-guard` là 2 hook. Mọi compressor là pure function, test bằng golden fixtures.

**Tech Stack:** Bun 1.3.x, TypeScript, `bun test`.

## Global Constraints

- Runtime: **Bun 1.3.x** (đã có `1.3.9`). Không thêm dependency runtime ngoài Bun built-ins.
- Ngôn ngữ: TypeScript strict.
- **Nguyên tắc vàng:** nén phần nhiễu, GIỮ phần tín hiệu (lỗi/fail in đầy đủ). Compressor throw → fallback in raw. **Exit code gốc luôn được bảo toàn.**
- Determinism: KHÔNG dùng `Date.now()`/`Math.random()` trong code path mà test assert; id được truyền vào (injectable).
- Commit message: Conventional Commits, tiếng Việt, **không** Co-Authored-By.
- Không `git push` (chỉ commit local).

---

## File Structure

| File | Trách nhiệm |
|------|-------------|
| `package.json`, `tsconfig.json` | scaffold, bin `kt`, script test |
| `src/compressors/types.ts` | `CompressInput`, `CompressResult`, `Compressor`, helper `joinOutput`/`countLines` |
| `src/compressors/generic.ts` | fallback: giữ head+tail, ẩn giữa |
| `src/compressors/test.ts` | nén output test runner |
| `src/compressors/build.ts` | nén output build |
| `src/compressors/install.ts` | nén output install |
| `src/compressors/git.ts` | nén `git diff`/`status` |
| `src/compressors/index.ts` | registry `compress(profile, input, config)` |
| `src/detect.ts` | command → `Profile` |
| `src/runner.ts` | spawn lệnh, capture, giữ exit code |
| `src/store.ts` | lưu/đọc full log `.kt/runs/` |
| `src/config.ts` | `Config`, `defaultConfig`, `loadConfig` |
| `src/statusline.ts` | `renderStatusline(input, cfg)` |
| `src/hooks/compress.ts` | `decideCompress(command)` |
| `src/hooks/guard.ts` | `decideGuard(command, cfg)` |
| `src/pipeline.ts` | `runAndCompress(argv, config, idFactory)` nối runner+detect+compress+store |
| `src/cli.ts` | parse argv, dispatch subcommand |
| `src/init.ts` | merge hooks+statusLine vào `~/.claude/settings.json` |
| `skills/concise-output.md` | skill output gọn (vẫn dễ đọc) |
| `test/fixtures/*` | output thật làm golden input |
| `README.md` | hướng dẫn + số đo tiết kiệm |

---

## Task 1: Scaffold project

**Files:**
- Create: `package.json`, `tsconfig.json`, `test/smoke.test.ts`

**Interfaces:**
- Produces: lệnh `bun test` chạy được; bin `kt` trỏ `src/cli.ts`.

- [ ] **Step 1: Tạo `package.json`**

```json
{
  "name": "kuro-lean",
  "version": "0.1.0",
  "type": "module",
  "bin": { "kt": "./src/cli.ts" },
  "scripts": {
    "test": "bun test",
    "kt": "bun run src/cli.ts"
  }
}
```

- [ ] **Step 2: Tạo `tsconfig.json`**

```json
{
  "compilerOptions": {
    "strict": true,
    "target": "ESNext",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "types": ["bun-types"],
    "skipLibCheck": true,
    "noUncheckedIndexedAccess": true
  }
}
```

- [ ] **Step 3: Viết smoke test `test/smoke.test.ts`**

```ts
import { test, expect } from "bun:test";

test("bun test chạy được", () => {
  expect(1 + 1).toBe(2);
});
```

- [ ] **Step 4: Chạy test**

Run: `cd /Users/kuro/Dev/kuro-lean && bun test`
Expected: PASS 1 test.

- [ ] **Step 5: Commit**

```bash
git add package.json tsconfig.json test/smoke.test.ts
git commit -m "chore: scaffold kuro-lean (bun + ts + bun test)"
```

---

## Task 2: Types + generic compressor

**Files:**
- Create: `src/compressors/types.ts`, `src/compressors/generic.ts`, `test/generic.test.ts`

**Interfaces:**
- Produces:
  - `interface CompressInput { stdout: string; stderr: string; exitCode: number; command: string }`
  - `interface CompressResult { text: string; originalLines: number; compactLines: number; note?: string }`
  - `type Compressor = (input: CompressInput, opts?: GenericOpts) => CompressResult`
  - `function joinOutput(i: CompressInput): string`
  - `function countLines(s: string): number`
  - `function generic(input: CompressInput, opts?: GenericOpts): CompressResult`
  - `interface GenericOpts { thresholdLines: number; headLines: number; tailLines: number }`

- [ ] **Step 1: Viết `src/compressors/types.ts`**

```ts
export interface CompressInput {
  stdout: string;
  stderr: string;
  exitCode: number;
  command: string;
}

export interface CompressResult {
  text: string;
  originalLines: number;
  compactLines: number;
  note?: string;
}

export interface GenericOpts {
  thresholdLines: number;
  headLines: number;
  tailLines: number;
}

export type Compressor = (input: CompressInput, opts?: GenericOpts) => CompressResult;

/** Gộp stdout + stderr, bỏ newline thừa cuối từng phần. */
export function joinOutput(i: CompressInput): string {
  return [i.stdout, i.stderr]
    .map((s) => s.replace(/\n+$/, ""))
    .filter((s) => s.length > 0)
    .join("\n");
}

export function countLines(s: string): number {
  return s.length === 0 ? 0 : s.split("\n").length;
}
```

- [ ] **Step 2: Viết test `test/generic.test.ts`**

```ts
import { test, expect } from "bun:test";
import { generic } from "../src/compressors/generic";
import type { CompressInput } from "../src/compressors/types";

const opts = { thresholdLines: 5, headLines: 2, tailLines: 1 };
const base = (stdout: string): CompressInput => ({ stdout, stderr: "", exitCode: 0, command: "x" });

test("dưới ngưỡng: giữ nguyên", () => {
  const r = generic(base("a\nb\nc"), opts);
  expect(r.text).toBe("a\nb\nc");
  expect(r.compactLines).toBe(3);
});

test("vượt ngưỡng: head + tail + dòng ẩn", () => {
  const r = generic(base("1\n2\n3\n4\n5\n6\n7\n8"), opts);
  expect(r.text.split("\n")[0]).toBe("1");
  expect(r.text).toContain("dòng đã ẩn");
  expect(r.text.split("\n").at(-1)).toBe("8");
  expect(r.originalLines).toBe(8);
});
```

- [ ] **Step 3: Chạy test (đỏ)**

Run: `bun test test/generic.test.ts`
Expected: FAIL — `Cannot find module ../src/compressors/generic`.

- [ ] **Step 4: Viết `src/compressors/generic.ts`**

```ts
import { type CompressInput, type CompressResult, type GenericOpts, joinOutput, countLines } from "./types";

const DEFAULT: GenericOpts = { thresholdLines: 40, headLines: 15, tailLines: 10 };

export function generic(input: CompressInput, opts: GenericOpts = DEFAULT): CompressResult {
  const combined = joinOutput(input);
  const total = countLines(combined);
  if (total <= opts.thresholdLines) {
    return { text: combined, originalLines: total, compactLines: total };
  }
  const lines = combined.split("\n");
  const head = lines.slice(0, opts.headLines);
  const tail = lines.slice(-opts.tailLines);
  const hidden = total - head.length - tail.length;
  const text = [...head, `… [${hidden} dòng đã ẩn — kt show] …`, ...tail].join("\n");
  return { text, originalLines: total, compactLines: head.length + tail.length + 1 };
}
```

- [ ] **Step 5: Chạy test (xanh)**

Run: `bun test test/generic.test.ts`
Expected: PASS 2 tests.

- [ ] **Step 6: Commit**

```bash
git add src/compressors/types.ts src/compressors/generic.ts test/generic.test.ts
git commit -m "feat: compressor types + generic head/tail"
```

---

## Task 3: detect (command → profile)

**Files:**
- Create: `src/detect.ts`, `test/detect.test.ts`

**Interfaces:**
- Produces:
  - `type Profile = "test" | "build" | "install" | "git" | "generic"`
  - `function detect(command: string): Profile`

- [ ] **Step 1: Viết test `test/detect.test.ts`**

```ts
import { test, expect } from "bun:test";
import { detect } from "../src/detect";

test.each([
  ["npm test", "test"],
  ["pnpm run test", "test"],
  ["vitest run", "test"],
  ["go test ./...", "test"],
  ["cargo build --release", "build"],
  ["tsc -p .", "build"],
  ["next build", "build"],
  ["npm install", "install"],
  ["pnpm add zod", "install"],
  ["bun install", "install"],
  ["git status", "git"],
  ["git diff HEAD~1", "git"],
  ["ls -la", "generic"],
  ["echo hi", "generic"],
])("detect(%s) => %s", (cmd, expected) => {
  expect(detect(cmd as string)).toBe(expected);
});
```

- [ ] **Step 2: Chạy test (đỏ)**

Run: `bun test test/detect.test.ts`
Expected: FAIL — module không tồn tại.

- [ ] **Step 3: Viết `src/detect.ts`**

```ts
export type Profile = "test" | "build" | "install" | "git" | "generic";

const PATTERNS: [Profile, RegExp][] = [
  ["test", /\b(jest|vitest|bun test|go test|cargo test|pytest|(npm|pnpm|yarn) (run )?test)\b/],
  ["build", /\b(tsc|cargo build|webpack|vite build|next build|go build)\b/],
  ["install", /\b((npm|pnpm) (i|install|add)|yarn (add|install)?|bun (i|install|add))\b/],
  ["git", /\bgit (status|diff|log)\b/],
];

export function detect(command: string): Profile {
  for (const [profile, re] of PATTERNS) {
    if (re.test(command)) return profile;
  }
  return "generic";
}
```

- [ ] **Step 4: Chạy test (xanh)**

Run: `bun test test/detect.test.ts`
Expected: PASS tất cả case.

- [ ] **Step 5: Commit**

```bash
git add src/detect.ts test/detect.test.ts
git commit -m "feat: detect profile từ command"
```

---

## Task 4: runner (spawn + giữ exit code)

**Files:**
- Create: `src/runner.ts`, `test/runner.test.ts`

**Interfaces:**
- Produces:
  - `interface RunResult { stdout: string; stderr: string; exitCode: number; durationMs: number }`
  - `function run(argv: string[]): Promise<RunResult>`

- [ ] **Step 1: Viết test `test/runner.test.ts`**

```ts
import { test, expect } from "bun:test";
import { run } from "../src/runner";

test("capture stdout + exit 0", async () => {
  const r = await run(["echo", "hello"]);
  expect(r.stdout.trim()).toBe("hello");
  expect(r.exitCode).toBe(0);
});

test("bảo toàn exit code khác 0", async () => {
  const r = await run(["sh", "-c", "echo oops >&2; exit 3"]);
  expect(r.exitCode).toBe(3);
  expect(r.stderr).toContain("oops");
});
```

- [ ] **Step 2: Chạy test (đỏ)**

Run: `bun test test/runner.test.ts`
Expected: FAIL — module không tồn tại.

- [ ] **Step 3: Viết `src/runner.ts`**

```ts
export interface RunResult {
  stdout: string;
  stderr: string;
  exitCode: number;
  durationMs: number;
}

export async function run(argv: string[]): Promise<RunResult> {
  const start = performance.now();
  const proc = Bun.spawn(argv, { stdout: "pipe", stderr: "pipe" });
  const [stdout, stderr] = await Promise.all([
    new Response(proc.stdout).text(),
    new Response(proc.stderr).text(),
  ]);
  const exitCode = await proc.exited;
  return { stdout, stderr, exitCode, durationMs: performance.now() - start };
}
```

- [ ] **Step 4: Chạy test (xanh)**

Run: `bun test test/runner.test.ts`
Expected: PASS 2 tests.

- [ ] **Step 5: Commit**

```bash
git add src/runner.ts test/runner.test.ts
git commit -m "feat: runner spawn + bảo toàn exit code"
```

---

## Task 5: store (lưu/đọc full log)

**Files:**
- Create: `src/store.ts`, `test/store.test.ts`

**Interfaces:**
- Produces:
  - `function saveRun(id: string, content: string, opts?: { keep?: number; root?: string }): string` (trả path đã ghi)
  - `function showRun(id?: string, root?: string): string | null`
  - `function listRuns(root?: string): string[]` (id sắp xếp tăng dần theo tên)

- [ ] **Step 1: Viết test `test/store.test.ts`**

```ts
import { test, expect } from "bun:test";
import { saveRun, showRun, listRuns } from "../src/store";
import { rmSync } from "fs";

const ROOT = "/tmp/kt-test-store";

test("save rồi show theo id", () => {
  rmSync(ROOT, { recursive: true, force: true });
  saveRun("001", "full log A", { root: ROOT });
  expect(showRun("001", ROOT)).toBe("full log A");
});

test("show không id => bản mới nhất; prune theo keep", () => {
  rmSync(ROOT, { recursive: true, force: true });
  saveRun("001", "A", { root: ROOT, keep: 2 });
  saveRun("002", "B", { root: ROOT, keep: 2 });
  saveRun("003", "C", { root: ROOT, keep: 2 });
  expect(showRun(undefined, ROOT)).toBe("C");
  expect(listRuns(ROOT)).toEqual(["002", "003"]);
});
```

- [ ] **Step 2: Chạy test (đỏ)**

Run: `bun test test/store.test.ts`
Expected: FAIL — module không tồn tại.

- [ ] **Step 3: Viết `src/store.ts`**

```ts
import { mkdirSync, writeFileSync, readFileSync, readdirSync, rmSync, existsSync } from "fs";
import { join } from "path";

const DEFAULT_ROOT = ".kt/runs";

export function listRuns(root: string = DEFAULT_ROOT): string[] {
  if (!existsSync(root)) return [];
  return readdirSync(root)
    .filter((f) => f.endsWith(".log"))
    .map((f) => f.slice(0, -4))
    .sort();
}

export function saveRun(id: string, content: string, opts: { keep?: number; root?: string } = {}): string {
  const root = opts.root ?? DEFAULT_ROOT;
  mkdirSync(root, { recursive: true });
  const path = join(root, `${id}.log`);
  writeFileSync(path, content);
  const keep = opts.keep ?? 50;
  const ids = listRuns(root);
  for (const old of ids.slice(0, Math.max(0, ids.length - keep))) {
    rmSync(join(root, `${old}.log`), { force: true });
  }
  return path;
}

export function showRun(id?: string, root: string = DEFAULT_ROOT): string | null {
  const target = id ?? listRuns(root).at(-1);
  if (!target) return null;
  const path = join(root, `${target}.log`);
  return existsSync(path) ? readFileSync(path, "utf8") : null;
}
```

- [ ] **Step 4: Chạy test (xanh)**

Run: `bun test test/store.test.ts`
Expected: PASS 2 tests.

- [ ] **Step 5: Commit**

```bash
git add src/store.ts test/store.test.ts
git commit -m "feat: store lưu/đọc full log + prune"
```

---

## Task 6: test compressor + fixtures

**Files:**
- Create: `src/compressors/test.ts`, `test/fixtures/vitest-pass.txt`, `test/fixtures/vitest-fail.txt`, `test/compress-test.test.ts`

**Interfaces:**
- Consumes: `CompressInput`, `CompressResult`, `joinOutput`, `countLines` (Task 2).
- Produces: `function compressTest(input: CompressInput): CompressResult`

- [ ] **Step 1: Tạo fixture `test/fixtures/vitest-pass.txt`**

```
 RUN  v1.6.0 /Users/kuro/Dev/app
 ✓ src/a.test.ts (3)
 ✓ src/b.test.ts (5)
 Test Files  2 passed (2)
      Tests  8 passed (8)
   Start at  10:00:00
   Duration  1.20s
```

- [ ] **Step 2: Tạo fixture `test/fixtures/vitest-fail.txt`**

```
 RUN  v1.6.0 /Users/kuro/Dev/app
 ✓ src/a.test.ts (3)
 FAIL  src/b.test.ts > tính tổng
AssertionError: expected 4 to be 5
 ❯ src/b.test.ts:12:20
 Test Files  1 failed | 1 passed (2)
      Tests  1 failed | 7 passed (8)
```

- [ ] **Step 3: Viết test `test/compress-test.test.ts`**

```ts
import { test, expect } from "bun:test";
import { readFileSync } from "fs";
import { compressTest } from "../src/compressors/test";
import type { CompressInput } from "../src/compressors/types";

const fx = (name: string) => readFileSync(`test/fixtures/${name}`, "utf8");

test("pass => 1 dòng summary, không kèm chi tiết test", () => {
  const input: CompressInput = { stdout: fx("vitest-pass.txt"), stderr: "", exitCode: 0, command: "vitest run" };
  const r = compressTest(input);
  expect(r.compactLines).toBeLessThanOrEqual(2);
  expect(r.text).toContain("8 passed");
});

test("fail => GIỮ block lỗi đầy đủ", () => {
  const input: CompressInput = { stdout: fx("vitest-fail.txt"), stderr: "", exitCode: 1, command: "vitest run" };
  const r = compressTest(input);
  expect(r.text).toContain("AssertionError: expected 4 to be 5");
  expect(r.text).toContain("FAIL");
  expect(r.text).toContain("src/b.test.ts:12:20");
});
```

- [ ] **Step 4: Chạy test (đỏ)**

Run: `bun test test/compress-test.test.ts`
Expected: FAIL — module không tồn tại.

- [ ] **Step 5: Viết `src/compressors/test.ts`**

```ts
import { type CompressInput, type CompressResult, joinOutput, countLines } from "./types";

const FAIL_RE = /(FAIL|✕|✗|✘|×|\bfailed\b|\bpanic\b|not ok|AssertionError|Error:)/i;
const SUMMARY_RE = /(Tests?\s+\d+\s+passed|Tests?\s+\d+\s+failed|test result:|\d+ passing|\d+ passed)/i;

export function compressTest(input: CompressInput): CompressResult {
  const combined = joinOutput(input);
  const lines = combined.split("\n");
  const total = countLines(combined);

  if (input.exitCode === 0) {
    const summary = lines.filter((l) => SUMMARY_RE.test(l)).at(-1);
    const text = summary?.trim() ?? `✓ tests passed (exit 0)`;
    return { text, originalLines: total, compactLines: countLines(text) };
  }

  // FAIL: giữ từ dấu hiệu lỗi đầu tiên tới cuối + mọi dòng summary phía trước.
  const firstFail = lines.findIndex((l) => FAIL_RE.test(l));
  if (firstFail === -1) {
    return { text: combined, originalLines: total, compactLines: total, note: "no-marker-fallback" };
  }
  const summaryBefore = lines.slice(0, firstFail).filter((l) => SUMMARY_RE.test(l));
  const kept = [...summaryBefore, ...lines.slice(firstFail)].join("\n");
  return { text: kept, originalLines: total, compactLines: countLines(kept) };
}
```

- [ ] **Step 6: Chạy test (xanh)**

Run: `bun test test/compress-test.test.ts`
Expected: PASS 2 tests.

- [ ] **Step 7: Commit**

```bash
git add src/compressors/test.ts test/fixtures/vitest-pass.txt test/fixtures/vitest-fail.txt test/compress-test.test.ts
git commit -m "feat: test compressor (pass=summary, fail=giữ lỗi)"
```

---

## Task 7: build compressor

**Files:**
- Create: `src/compressors/build.ts`, `test/compress-build.test.ts`

**Interfaces:**
- Consumes: `CompressInput`, `CompressResult`, `joinOutput`, `countLines` (Task 2).
- Produces: `function compressBuild(input: CompressInput): CompressResult`

- [ ] **Step 1: Viết test `test/compress-build.test.ts`**

```ts
import { test, expect } from "bun:test";
import { compressBuild } from "../src/compressors/build";
import type { CompressInput } from "../src/compressors/types";

test("build OK => 1 dòng", () => {
  const input: CompressInput = {
    stdout: "compiling...\nlinking...\nwrote dist/index.js\ndone in 2s",
    stderr: "", exitCode: 0, command: "tsc -p .",
  };
  const r = compressBuild(input);
  expect(r.compactLines).toBe(1);
  expect(r.text).toContain("OK");
});

test("có error => giữ dòng error", () => {
  const input: CompressInput = {
    stdout: "compiling...\nsrc/a.ts(3,5): error TS2304: Cannot find name 'x'.\nmore noise\nmore noise",
    stderr: "", exitCode: 2, command: "tsc -p .",
  };
  const r = compressBuild(input);
  expect(r.text).toContain("error TS2304");
  expect(r.text).not.toContain("more noise");
});
```

- [ ] **Step 2: Chạy test (đỏ)**

Run: `bun test test/compress-build.test.ts`
Expected: FAIL — module không tồn tại.

- [ ] **Step 3: Viết `src/compressors/build.ts`**

```ts
import { type CompressInput, type CompressResult, joinOutput, countLines } from "./types";

const ISSUE_RE = /\b(error|warning)\b/i;

export function compressBuild(input: CompressInput): CompressResult {
  const combined = joinOutput(input);
  const total = countLines(combined);
  const issues = combined.split("\n").filter((l) => ISSUE_RE.test(l));

  if (input.exitCode === 0 && issues.length === 0) {
    const text = `✓ build OK (${total} dòng đã ẩn)`;
    return { text, originalLines: total, compactLines: 1 };
  }
  if (issues.length === 0) {
    return { text: combined, originalLines: total, compactLines: total, note: "no-issue-fallback" };
  }
  const text = issues.join("\n");
  return { text, originalLines: total, compactLines: issues.length };
}
```

- [ ] **Step 4: Chạy test (xanh)**

Run: `bun test test/compress-build.test.ts`
Expected: PASS 2 tests.

- [ ] **Step 5: Commit**

```bash
git add src/compressors/build.ts test/compress-build.test.ts
git commit -m "feat: build compressor (OK=1 dòng, lỗi=giữ error/warning)"
```

---

## Task 8: install compressor

**Files:**
- Create: `src/compressors/install.ts`, `test/compress-install.test.ts`

**Interfaces:**
- Consumes: `CompressInput`, `CompressResult`, `joinOutput`, `countLines` (Task 2).
- Produces: `function compressInstall(input: CompressInput): CompressResult`

- [ ] **Step 1: Viết test `test/compress-install.test.ts`**

```ts
import { test, expect } from "bun:test";
import { compressInstall } from "../src/compressors/install";
import type { CompressInput } from "../src/compressors/types";

test("giữ dòng kết quả + warn, bỏ progress", () => {
  const input: CompressInput = {
    stdout: [
      "Resolving dependencies",
      "Fetching package a",
      "Fetching package b",
      "npm warn deprecated foo@1.0.0",
      "added 120 packages in 4s",
    ].join("\n"),
    stderr: "", exitCode: 0, command: "npm install",
  };
  const r = compressInstall(input);
  expect(r.text).toContain("added 120 packages");
  expect(r.text).toContain("deprecated foo");
  expect(r.text).not.toContain("Fetching package");
});
```

- [ ] **Step 2: Chạy test (đỏ)**

Run: `bun test test/compress-install.test.ts`
Expected: FAIL — module không tồn tại.

- [ ] **Step 3: Viết `src/compressors/install.ts`**

```ts
import { type CompressInput, type CompressResult, joinOutput, countLines } from "./types";

const KEEP_RE = /\b(added|removed|changed|audited|packages|warn|error|deprecated|peer|vulnerabilit)/i;

export function compressInstall(input: CompressInput): CompressResult {
  const combined = joinOutput(input);
  const total = countLines(combined);
  const lines = combined.split("\n");
  const kept = lines.filter((l) => KEEP_RE.test(l));
  const lastLine = lines.at(-1);
  if (lastLine && !kept.includes(lastLine)) kept.push(lastLine);
  const text = kept.length ? kept.join("\n") : combined;
  return { text, originalLines: total, compactLines: countLines(text) };
}
```

- [ ] **Step 4: Chạy test (xanh)**

Run: `bun test test/compress-install.test.ts`
Expected: PASS 1 test.

- [ ] **Step 5: Commit**

```bash
git add src/compressors/install.ts test/compress-install.test.ts
git commit -m "feat: install compressor (giữ kết quả/warn, bỏ progress)"
```

---

## Task 9: git compressor

**Files:**
- Create: `src/compressors/git.ts`, `test/fixtures/git-diff.txt`, `test/compress-git.test.ts`

**Interfaces:**
- Consumes: `CompressInput`, `CompressResult`, `joinOutput`, `countLines`, `generic` (Task 2).
- Produces: `function compressGit(input: CompressInput, opts?: { thresholdLines: number; headLines: number; tailLines: number }): CompressResult`

- [ ] **Step 1: Tạo fixture `test/fixtures/git-diff.txt`**

```
diff --git a/src/a.ts b/src/a.ts
index 111..222 100644
--- a/src/a.ts
+++ b/src/a.ts
@@ -1,3 +1,4 @@
 const x = 1;
+const y = 2;
 const z = 3;
-const w = 4;
diff --git a/src/b.ts b/src/b.ts
index 333..444 100644
--- a/src/b.ts
+++ b/src/b.ts
@@ -10,2 +10,2 @@
-old line
+new line
```

- [ ] **Step 2: Viết test `test/compress-git.test.ts`**

```ts
import { test, expect } from "bun:test";
import { readFileSync } from "fs";
import { compressGit } from "../src/compressors/git";
import type { CompressInput } from "../src/compressors/types";

test("git diff => per-file +/- stat", () => {
  const input: CompressInput = {
    stdout: readFileSync("test/fixtures/git-diff.txt", "utf8"),
    stderr: "", exitCode: 0, command: "git diff",
  };
  const r = compressGit(input);
  expect(r.text).toContain("src/a.ts");
  expect(r.text).toContain("+1 -1");   // a.ts: 1 add, 1 remove
  expect(r.text).toContain("src/b.ts");
  expect(r.text).not.toContain("@@");  // hunk header bị lược
});

test("git status nhỏ => giữ nguyên (generic)", () => {
  const input: CompressInput = {
    stdout: "On branch main\nnothing to commit, working tree clean",
    stderr: "", exitCode: 0, command: "git status",
  };
  const r = compressGit(input);
  expect(r.text).toContain("working tree clean");
});
```

- [ ] **Step 3: Chạy test (đỏ)**

Run: `bun test test/compress-git.test.ts`
Expected: FAIL — module không tồn tại.

- [ ] **Step 4: Viết `src/compressors/git.ts`**

```ts
import { type CompressInput, type CompressResult, joinOutput, countLines } from "./types";
import { generic } from "./generic";

export function compressGit(
  input: CompressInput,
  opts = { thresholdLines: 40, headLines: 15, tailLines: 10 },
): CompressResult {
  const combined = joinOutput(input);
  const total = countLines(combined);

  if (!/diff --git/.test(combined)) {
    // status/log: để generic xử lý (nhỏ thì giữ nguyên)
    return generic(input, opts);
  }

  // diff: gom theo file, đếm +/-
  const files: { path: string; add: number; del: number }[] = [];
  let cur: { path: string; add: number; del: number } | null = null;
  for (const line of combined.split("\n")) {
    const m = line.match(/^diff --git a\/(.+?) b\//);
    if (m) {
      cur = { path: m[1]!, add: 0, del: 0 };
      files.push(cur);
      continue;
    }
    if (!cur) continue;
    if (line.startsWith("+") && !line.startsWith("+++")) cur.add++;
    else if (line.startsWith("-") && !line.startsWith("---")) cur.del++;
  }
  const text = files.map((f) => `${f.path}  +${f.add} -${f.del}`).join("\n");
  return { text, originalLines: total, compactLines: files.length };
}
```

- [ ] **Step 5: Chạy test (xanh)**

Run: `bun test test/compress-git.test.ts`
Expected: PASS 2 tests.

- [ ] **Step 6: Commit**

```bash
git add src/compressors/git.ts test/fixtures/git-diff.txt test/compress-git.test.ts
git commit -m "feat: git compressor (diff => per-file stat)"
```

---

## Task 10: config + compressor registry

**Files:**
- Create: `src/config.ts`, `src/compressors/index.ts`, `test/config.test.ts`, `test/registry.test.ts`

**Interfaces:**
- Consumes: tất cả compressor (Task 2,6,7,8,9), `detect` `Profile` (Task 3).
- Produces:
  - `interface Config { profiles: Record<Profile, boolean>; generic: GenericOpts; git: { maxHunkLinesPerFile: number }; store: { keepRuns: number }; statusline: { warnPct: number; dangerPct: number }; guard: GuardConfig }`
  - `const defaultConfig: Config`
  - `function loadConfig(cwd?: string): Config`
  - `function compress(profile: Profile, input: CompressInput, config: Config): CompressResult`
  - `interface GuardConfig { maxCatKb: number; rules: Record<string, boolean> }`

- [ ] **Step 1: Viết test `test/config.test.ts`**

```ts
import { test, expect } from "bun:test";
import { writeFileSync, mkdirSync, rmSync } from "fs";
import { loadConfig, defaultConfig } from "../src/config";

const DIR = "/tmp/kt-test-config";

test("không có kt.json => default", () => {
  rmSync(DIR, { recursive: true, force: true });
  mkdirSync(DIR, { recursive: true });
  expect(loadConfig(DIR).generic.thresholdLines).toBe(defaultConfig.generic.thresholdLines);
});

test("kt.json override merge nông", () => {
  rmSync(DIR, { recursive: true, force: true });
  mkdirSync(DIR, { recursive: true });
  writeFileSync(`${DIR}/kt.json`, JSON.stringify({ generic: { thresholdLines: 5, headLines: 2, tailLines: 1 } }));
  expect(loadConfig(DIR).generic.thresholdLines).toBe(5);
  expect(loadConfig(DIR).store.keepRuns).toBe(defaultConfig.store.keepRuns);
});
```

- [ ] **Step 2: Viết test `test/registry.test.ts`**

```ts
import { test, expect } from "bun:test";
import { compress } from "../src/compressors";
import { defaultConfig } from "../src/config";
import type { CompressInput } from "../src/compressors/types";

test("profile tắt => generic", () => {
  const cfg = { ...defaultConfig, profiles: { ...defaultConfig.profiles, test: false } };
  const input: CompressInput = { stdout: "a\nb", stderr: "", exitCode: 0, command: "npm test" };
  const r = compress("test", input, cfg);
  expect(r.text).toBe("a\nb"); // generic dưới ngưỡng giữ nguyên
});

test("route đúng compressor: build có error => giữ dòng error", () => {
  const input: CompressInput = {
    stdout: "noise\nsrc/a.ts(3,5): error TS2304: Cannot find name 'x'.\nnoise",
    stderr: "", exitCode: 2, command: "tsc -p .",
  };
  const r = compress("build", input, defaultConfig);
  expect(r.text).toContain("error TS2304");
  expect(r.text).not.toContain("noise");
});
```

- [ ] **Step 3: Chạy test (đỏ)**

Run: `bun test test/config.test.ts test/registry.test.ts`
Expected: FAIL — module không tồn tại.

- [ ] **Step 4: Viết `src/config.ts`**

```ts
import { existsSync, readFileSync } from "fs";
import { join } from "path";
import type { Profile } from "./detect";
import type { GenericOpts } from "./compressors/types";

export interface GuardConfig {
  maxCatKb: number;
  rules: Record<string, boolean>;
}

export interface Config {
  profiles: Record<Profile, boolean>;
  generic: GenericOpts;
  git: { maxHunkLinesPerFile: number };
  store: { keepRuns: number };
  statusline: { warnPct: number; dangerPct: number };
  guard: GuardConfig;
}

export const defaultConfig: Config = {
  profiles: { test: true, build: true, install: true, git: true, generic: true },
  generic: { thresholdLines: 40, headLines: 15, tailLines: 10 },
  git: { maxHunkLinesPerFile: 20 },
  store: { keepRuns: 50 },
  statusline: { warnPct: 60, dangerPct: 85 },
  guard: { maxCatKb: 100, rules: { findRoot: true, npmLs: true, treeNoDepth: true, catBig: true } },
};

export function loadConfig(cwd: string = process.cwd()): Config {
  const path = join(cwd, "kt.json");
  if (!existsSync(path)) return defaultConfig;
  try {
    const user = JSON.parse(readFileSync(path, "utf8")) as Partial<Config>;
    return {
      ...defaultConfig,
      ...user,
      profiles: { ...defaultConfig.profiles, ...user.profiles },
      generic: { ...defaultConfig.generic, ...user.generic },
      git: { ...defaultConfig.git, ...user.git },
      store: { ...defaultConfig.store, ...user.store },
      statusline: { ...defaultConfig.statusline, ...user.statusline },
      guard: { ...defaultConfig.guard, ...user.guard, rules: { ...defaultConfig.guard.rules, ...user.guard?.rules } },
    };
  } catch {
    return defaultConfig;
  }
}
```

- [ ] **Step 5: Viết `src/compressors/index.ts`**

```ts
import type { Profile } from "../detect";
import type { Config } from "../config";
import { type CompressInput, type CompressResult, joinOutput, countLines } from "./types";
import { generic } from "./generic";
import { compressTest } from "./test";
import { compressBuild } from "./build";
import { compressInstall } from "./install";
import { compressGit } from "./git";

export function compress(profile: Profile, input: CompressInput, config: Config): CompressResult {
  const useProfile = config.profiles[profile] ? profile : "generic";
  try {
    switch (useProfile) {
      case "test": return compressTest(input);
      case "build": return compressBuild(input);
      case "install": return compressInstall(input);
      case "git": return compressGit(input, config.generic);
      default: return generic(input, config.generic);
    }
  } catch {
    const raw = joinOutput(input);
    return { text: raw, originalLines: countLines(raw), compactLines: countLines(raw), note: "compressor-error-fallback" };
  }
}
```

- [ ] **Step 6: Chạy test (xanh)**

Run: `bun test test/config.test.ts test/registry.test.ts`
Expected: PASS.

- [ ] **Step 7: Commit**

```bash
git add src/config.ts src/compressors/index.ts test/config.test.ts test/registry.test.ts
git commit -m "feat: config + compressor registry (fallback raw an toàn)"
```

---

## Task 11: statusline render + pipeline

**Files:**
- Create: `src/statusline.ts`, `src/pipeline.ts`, `test/statusline.test.ts`, `test/pipeline.test.ts`

**Interfaces:**
- Consumes: `run` (Task 4), `detect` (Task 3), `compress` (Task 10), `saveRun` (Task 5), `Config`/`loadConfig` (Task 10).
- Produces:
  - `interface StatuslineInput { context_window?: {...}; model?: {...}; cost?: {...} }`
  - `function renderStatusline(input: StatuslineInput, cfg: { warnPct: number; dangerPct: number }): string`
  - `function runAndCompress(argv: string[], config: Config, idFactory: () => string): Promise<{ compact: string; exitCode: number }>`

- [ ] **Step 1: Viết test `test/statusline.test.ts`**

```ts
import { test, expect } from "bun:test";
import { renderStatusline } from "../src/statusline";

const cfg = { warnPct: 60, dangerPct: 85 };

test("dưới warn => xanh + token + cost", () => {
  const s = renderStatusline(
    { context_window: { used_percentage: 42, total_input_tokens: 80000, total_output_tokens: 4000, context_window_size: 200000 }, model: { display_name: "Opus" }, cost: { total_cost_usd: 0.31 } },
    cfg,
  );
  expect(s).toContain("🟢");
  expect(s).toContain("42%");
  expect(s).toContain("84k");
  expect(s).toContain("$0.31");
});

test("≥ danger => đỏ", () => {
  const s = renderStatusline({ context_window: { used_percentage: 90, context_window_size: 200000 } }, cfg);
  expect(s).toContain("🔴");
});

test("thiếu field => không crash", () => {
  const s = renderStatusline({}, cfg);
  expect(typeof s).toBe("string");
});
```

- [ ] **Step 2: Viết test `test/pipeline.test.ts`**

```ts
import { test, expect } from "bun:test";
import { runAndCompress } from "../src/pipeline";
import { defaultConfig } from "../src/config";
import { showRun } from "../src/store";
import { rmSync } from "fs";

test("chạy lệnh => nén + lưu full + giữ exit code", async () => {
  rmSync(".kt/runs", { recursive: true, force: true });
  const r = await runAndCompress(["sh", "-c", "exit 0"], defaultConfig, () => "pipe001");
  expect(r.exitCode).toBe(0);
  expect(showRun("pipe001")).not.toBeNull();
});

test("exit code khác 0 được truyền ra", async () => {
  const r = await runAndCompress(["sh", "-c", "exit 7"], defaultConfig, () => "pipe002");
  expect(r.exitCode).toBe(7);
});
```

- [ ] **Step 3: Chạy test (đỏ)**

Run: `bun test test/statusline.test.ts test/pipeline.test.ts`
Expected: FAIL — module không tồn tại.

- [ ] **Step 4: Viết `src/statusline.ts`**

```ts
export interface StatuslineInput {
  context_window?: {
    used_percentage?: number;
    total_input_tokens?: number;
    total_output_tokens?: number;
    context_window_size?: number;
  };
  model?: { display_name?: string };
  cost?: { total_cost_usd?: number };
}

export function renderStatusline(input: StatuslineInput, cfg: { warnPct: number; dangerPct: number }): string {
  const cw = input.context_window ?? {};
  const size = cw.context_window_size ?? 0;
  const tokens = (cw.total_input_tokens ?? 0) + (cw.total_output_tokens ?? 0);
  const pct = cw.used_percentage ?? (size > 0 ? Math.round((tokens / size) * 100) : undefined);

  const dot = pct === undefined ? "⚪" : pct >= cfg.dangerPct ? "🔴" : pct >= cfg.warnPct ? "🟡" : "🟢";
  const pctStr = pct === undefined ? "~? ctx" : `${pct}% ctx`;
  const tokStr = tokens > 0 ? ` · ~${Math.round(tokens / 1000)}k tok` : "";
  const costStr = input.cost?.total_cost_usd !== undefined ? ` · $${input.cost.total_cost_usd.toFixed(2)}` : "";
  const model = input.model?.display_name ? `${input.model.display_name} · ` : "";
  return `${dot} ${model}${pctStr}${tokStr}${costStr}`;
}
```

- [ ] **Step 5: Viết `src/pipeline.ts`**

```ts
import { run } from "./runner";
import { detect } from "./detect";
import { compress } from "./compressors";
import { joinOutput, countLines } from "./compressors/types";
import { saveRun } from "./store";
import type { Config } from "./config";

export async function runAndCompress(
  argv: string[],
  config: Config,
  idFactory: () => string,
): Promise<{ compact: string; exitCode: number }> {
  const res = await run(argv);
  const command = argv.join(" ");
  const input = { stdout: res.stdout, stderr: res.stderr, exitCode: res.exitCode, command };
  const result = compress(detect(command), input, config);

  const id = idFactory();
  const full = joinOutput(input);
  try {
    saveRun(id, full, { keep: config.store.keepRuns });
  } catch {
    // ghi full thất bại: vẫn in compact
  }
  const saved = countLines(full) - result.compactLines;
  const footer = saved > 0 ? `\n↳ ${saved} dòng đã nén · full: kt show ${id}` : "";
  return { compact: result.text + footer, exitCode: res.exitCode };
}
```

- [ ] **Step 6: Chạy test (xanh)**

Run: `bun test test/statusline.test.ts test/pipeline.test.ts`
Expected: PASS.

- [ ] **Step 7: Commit**

```bash
git add src/statusline.ts src/pipeline.ts test/statusline.test.ts test/pipeline.test.ts
git commit -m "feat: statusline render + pipeline runAndCompress"
```

---

## Task 12: hook-compress logic

**Files:**
- Create: `src/hooks/compress.ts`, `test/hook-compress.test.ts`

**Interfaces:**
- Consumes: `detect` (Task 3).
- Produces: `function decideCompress(command: string): string | null` (trả command mới hoặc null nếu không rewrite)

- [ ] **Step 1: Viết test `test/hook-compress.test.ts`**

```ts
import { test, expect } from "bun:test";
import { decideCompress } from "../src/hooks/compress";

test("lệnh test => rewrite sang kt run", () => {
  expect(decideCompress("npm test")).toBe("kt run -- npm test");
});

test("đã là kt => bỏ qua", () => {
  expect(decideCompress("kt run -- npm test")).toBeNull();
});

test("có pipe/redirect/&& => bỏ qua (tránh phá logic)", () => {
  expect(decideCompress("npm test | tee out.txt")).toBeNull();
  expect(decideCompress("npm test && echo ok")).toBeNull();
  expect(decideCompress("npm test > out.txt")).toBeNull();
});

test("generic không match => null", () => {
  expect(decideCompress("echo hi")).toBeNull();
});
```

- [ ] **Step 2: Chạy test (đỏ)**

Run: `bun test test/hook-compress.test.ts`
Expected: FAIL — module không tồn tại.

- [ ] **Step 3: Viết `src/hooks/compress.ts`**

```ts
import { detect } from "../detect";

const COMPLEX_RE = /[|&;><`$]|\$\(/;

export function decideCompress(command: string): string | null {
  const cmd = command.trim();
  if (cmd.startsWith("kt ")) return null;
  if (COMPLEX_RE.test(cmd)) return null;
  if (detect(cmd) === "generic") return null;
  return `kt run -- ${cmd}`;
}
```

- [ ] **Step 4: Chạy test (xanh)**

Run: `bun test test/hook-compress.test.ts`
Expected: PASS 4 tests.

- [ ] **Step 5: Commit**

```bash
git add src/hooks/compress.ts test/hook-compress.test.ts
git commit -m "feat: hook-compress decideCompress (rewrite an toàn)"
```

---

## Task 13: hook-guard logic

**Files:**
- Create: `src/hooks/guard.ts`, `test/hook-guard.test.ts`

**Interfaces:**
- Consumes: `GuardConfig` (Task 10).
- Produces: `function decideGuard(command: string, cfg: GuardConfig): { deny: boolean; reason?: string }`

- [ ] **Step 1: Viết test `test/hook-guard.test.ts`**

```ts
import { test, expect } from "bun:test";
import { decideGuard } from "../src/hooks/guard";
import { defaultConfig } from "../src/config";

const g = defaultConfig.guard;

test("find / không scope => deny + gợi ý", () => {
  const r = decideGuard("find / -name foo", g);
  expect(r.deny).toBe(true);
  expect(r.reason).toContain("find");
});

test("npm ls không depth => deny", () => {
  expect(decideGuard("npm ls", g).deny).toBe(true);
});

test("tree không -L => deny", () => {
  expect(decideGuard("tree", g).deny).toBe(true);
});

test("lệnh an toàn => allow", () => {
  expect(decideGuard("ls -la", g).deny).toBe(false);
  expect(decideGuard("find ./src -name '*.ts'", g).deny).toBe(false);
});
```

- [ ] **Step 2: Chạy test (đỏ)**

Run: `bun test test/hook-guard.test.ts`
Expected: FAIL — module không tồn tại.

- [ ] **Step 3: Viết `src/hooks/guard.ts`**

```ts
import type { GuardConfig } from "../config";

interface Rule {
  key: string;
  test: (cmd: string) => boolean;
  reason: string;
}

const RULES: Rule[] = [
  {
    key: "findRoot",
    test: (c) => /\bfind\s+\/(\s|$)/.test(c),
    reason: "`find /` quét toàn ổ đĩa → cực ngốn token. Hãy scope: find ./thư-mục ...",
  },
  {
    key: "npmLs",
    test: (c) => /\bnpm ls\b/.test(c) && !/--depth/.test(c),
    reason: "`npm ls` in cả cây phụ thuộc. Thêm --depth=0 cho gọn.",
  },
  {
    key: "treeNoDepth",
    test: (c) => /\btree\b/.test(c) && !/-L\s*\d/.test(c),
    reason: "`tree` không giới hạn độ sâu. Thêm -L 2.",
  },
];

export function decideGuard(command: string, cfg: GuardConfig): { deny: boolean; reason?: string } {
  for (const rule of RULES) {
    if (cfg.rules[rule.key] && rule.test(command)) {
      return { deny: true, reason: rule.reason };
    }
  }
  return { deny: false };
}
```

- [ ] **Step 4: Chạy test (xanh)**

Run: `bun test test/hook-guard.test.ts`
Expected: PASS 4 tests.

- [ ] **Step 5: Commit**

```bash
git add src/hooks/guard.ts test/hook-guard.test.ts
git commit -m "feat: hook-guard decideGuard (chặn lệnh ngốn token)"
```

---

## Task 14: cli wiring

**Files:**
- Create: `src/cli.ts`
- Modify: không

**Interfaces:**
- Consumes: `runAndCompress`/`renderStatusline` (Task 11), `showRun` (Task 5), `decideCompress` (Task 12), `decideGuard` (Task 13), `loadConfig` (Task 10).
- Produces: binary `kt` với subcommand `run | status | hook-compress | hook-guard | show | doctor`.

- [ ] **Step 1: Viết `src/cli.ts`**

```ts
#!/usr/bin/env bun
import { runAndCompress } from "./pipeline";
import { renderStatusline, type StatuslineInput } from "./statusline";
import { showRun } from "./store";
import { decideCompress } from "./hooks/compress";
import { decideGuard } from "./hooks/guard";
import { loadConfig } from "./config";

async function readStdin(): Promise<string> {
  const chunks: Uint8Array[] = [];
  for await (const chunk of Bun.stdin.stream()) chunks.push(chunk);
  return Buffer.concat(chunks).toString("utf8");
}

function timestampId(): string {
  return new Date().toISOString().replace(/[:.]/g, "-");
}

async function main() {
  const [cmd, ...rest] = Bun.argv.slice(2);
  const config = loadConfig();

  switch (cmd) {
    case "run": {
      // kt run -- <command...>
      const argv = rest[0] === "--" ? rest.slice(1) : rest;
      if (process.env.KT_RAW === "1" || argv.length === 0) {
        const proc = Bun.spawn(argv, { stdout: "inherit", stderr: "inherit" });
        process.exit(await proc.exited);
      }
      const { compact, exitCode } = await runAndCompress(argv, config, timestampId);
      process.stdout.write(compact + "\n");
      process.exit(exitCode);
    }
    case "status": {
      const input = JSON.parse((await readStdin()) || "{}") as StatuslineInput;
      process.stdout.write(renderStatusline(input, config.statusline));
      return;
    }
    case "hook-compress": {
      const input = JSON.parse((await readStdin()) || "{}");
      const command: string = input?.tool_input?.command ?? "";
      const next = decideCompress(command);
      if (next) {
        process.stdout.write(JSON.stringify({
          hookSpecificOutput: { hookEventName: "PreToolUse", updatedInput: { command: next } },
        }));
      }
      return;
    }
    case "hook-guard": {
      const input = JSON.parse((await readStdin()) || "{}");
      const command: string = input?.tool_input?.command ?? "";
      const { deny, reason } = decideGuard(command, config.guard);
      if (deny) {
        process.stdout.write(JSON.stringify({
          hookSpecificOutput: { hookEventName: "PreToolUse", permissionDecision: "deny", permissionDecisionReason: reason },
        }));
      }
      return;
    }
    case "show": {
      const out = showRun(rest[0]);
      process.stdout.write(out ?? "(không có log)\n");
      return;
    }
    case "doctor": {
      const { runDoctor } = await import("./init");
      process.stdout.write(runDoctor());
      return;
    }
    default:
      process.stdout.write("kt <run|status|hook-compress|hook-guard|show|doctor>\n");
  }
}

main();
```

- [ ] **Step 2: Smoke test thủ công — `kt run`**

Run: `bun run src/cli.ts run -- sh -c "for i in $(seq 1 60); do echo line $i; done"`
Expected: in bản nén (head+tail + "dòng đã ẩn"), kết thúc bằng `↳ ... full: kt show <id>`, exit 0.

- [ ] **Step 3: Smoke test thủ công — `kt status`**

Run: `echo '{"context_window":{"used_percentage":42,"total_input_tokens":80000,"context_window_size":200000},"cost":{"total_cost_usd":0.31}}' | bun run src/cli.ts status`
Expected: `🟢 42% ctx · ~80k tok · $0.31`.

- [ ] **Step 4: Smoke test thủ công — `kt hook-compress`**

Run: `echo '{"tool_input":{"command":"npm test"}}' | bun run src/cli.ts hook-compress`
Expected: JSON chứa `"updatedInput":{"command":"kt run -- npm test"}`.

- [ ] **Step 5: Commit**

```bash
git add src/cli.ts
git commit -m "feat: cli wiring (run/status/hook-compress/hook-guard/show/doctor)"
```

---

## Task 15: `kt init` + doctor (cài vào settings.json)

**Files:**
- Create: `src/init.ts`, `test/init.test.ts`
- Modify: `src/cli.ts` (thêm case `init`)

**Interfaces:**
- Consumes: không (đọc/ghi file).
- Produces:
  - `function installSettings(settingsPath: string, ktBin: string): { changed: boolean; backup?: string }`
  - `function runDoctor(home?: string): string`

- [ ] **Step 1: Viết test `test/init.test.ts`**

```ts
import { test, expect } from "bun:test";
import { writeFileSync, mkdirSync, rmSync, readFileSync, existsSync } from "fs";
import { installSettings } from "../src/init";

const DIR = "/tmp/kt-test-init";
const settings = `${DIR}/settings.json`;

test("settings trống => thêm hook + statusLine", () => {
  rmSync(DIR, { recursive: true, force: true });
  mkdirSync(DIR, { recursive: true });
  writeFileSync(settings, "{}");
  const r = installSettings(settings, "kt");
  expect(r.changed).toBe(true);
  const cfg = JSON.parse(readFileSync(settings, "utf8"));
  expect(cfg.statusLine.command).toContain("kt status");
  const hooks = cfg.hooks.PreToolUse[0].hooks.map((h: any) => h.command);
  expect(hooks).toContain("kt hook-guard");
  expect(hooks).toContain("kt hook-compress");
});

test("idempotent: chạy lần 2 không nhân đôi", () => {
  installSettings(settings, "kt");
  const before = readFileSync(settings, "utf8");
  const r = installSettings(settings, "kt");
  expect(r.changed).toBe(false);
  expect(readFileSync(settings, "utf8")).toBe(before);
});

test("giữ nguyên hook/config có sẵn của user", () => {
  rmSync(DIR, { recursive: true, force: true });
  mkdirSync(DIR, { recursive: true });
  writeFileSync(settings, JSON.stringify({ env: { FOO: "bar" }, hooks: { PreToolUse: [{ matcher: "Edit", hooks: [{ type: "command", command: "my-hook" }] }] } }));
  installSettings(settings, "kt");
  const cfg = JSON.parse(readFileSync(settings, "utf8"));
  expect(cfg.env.FOO).toBe("bar");
  expect(JSON.stringify(cfg.hooks.PreToolUse)).toContain("my-hook");
  expect(existsSync(`${settings}.bak`)).toBe(true);
});
```

- [ ] **Step 2: Chạy test (đỏ)**

Run: `bun test test/init.test.ts`
Expected: FAIL — module không tồn tại.

- [ ] **Step 3: Viết `src/init.ts`**

```ts
import { readFileSync, writeFileSync, existsSync, copyFileSync } from "fs";
import { homedir } from "os";
import { join } from "path";

interface HookEntry { type: "command"; command: string }
interface Matcher { matcher: string; hooks: HookEntry[] }

export function installSettings(settingsPath: string, ktBin: string): { changed: boolean; backup?: string } {
  const settings: any = existsSync(settingsPath)
    ? JSON.parse(readFileSync(settingsPath, "utf8") || "{}")
    : {};

  const before = JSON.stringify(settings);

  // statusLine
  const wantStatus = `${ktBin} status`;
  if (settings.statusLine?.command !== wantStatus) {
    settings.statusLine = { type: "command", command: wantStatus, padding: 2 };
  }

  // hooks.PreToolUse (matcher Bash): guard trước, compress sau
  settings.hooks ??= {};
  settings.hooks.PreToolUse ??= [];
  const wanted = [`${ktBin} hook-guard`, `${ktBin} hook-compress`];
  let bashBlock: Matcher | undefined = settings.hooks.PreToolUse.find((m: Matcher) => m.matcher === "Bash" && m.hooks?.some((h) => h.command.startsWith(ktBin)));
  if (!bashBlock) {
    bashBlock = { matcher: "Bash", hooks: [] };
    settings.hooks.PreToolUse.push(bashBlock);
  }
  for (const cmd of wanted) {
    if (!bashBlock.hooks.some((h) => h.command === cmd)) {
      bashBlock.hooks.push({ type: "command", command: cmd });
    }
  }

  const after = JSON.stringify(settings);
  if (after === before) return { changed: false };

  let backup: string | undefined;
  if (existsSync(settingsPath)) {
    backup = `${settingsPath}.bak`;
    copyFileSync(settingsPath, backup);
  }
  writeFileSync(settingsPath, JSON.stringify(settings, null, 2));
  return { changed: true, backup };
}

export function runDoctor(home: string = homedir()): string {
  const settingsPath = join(home, ".claude", "settings.json");
  const lines: string[] = [];
  lines.push(`settings: ${existsSync(settingsPath) ? "✓ " + settingsPath : "✗ chưa có"}`);
  if (existsSync(settingsPath)) {
    const cfg = JSON.parse(readFileSync(settingsPath, "utf8") || "{}");
    const hasStatus = String(cfg.statusLine?.command ?? "").includes("kt status");
    const cmds = (cfg.hooks?.PreToolUse ?? []).flatMap((m: Matcher) => m.hooks?.map((h) => h.command) ?? []);
    lines.push(`statusLine kt: ${hasStatus ? "✓" : "✗"}`);
    lines.push(`hook-guard:    ${cmds.includes("kt hook-guard") ? "✓" : "✗"}`);
    lines.push(`hook-compress: ${cmds.includes("kt hook-compress") ? "✓" : "✗"}`);
  }
  lines.push(`bun: ${Bun.version}`);
  return lines.join("\n") + "\n";
}
```

- [ ] **Step 4: Thêm case `init` vào `src/cli.ts`** (ngay trước `case "doctor"`)

```ts
    case "init": {
      const { installSettings } = await import("./init");
      const { homedir } = await import("os");
      const { join } = await import("path");
      const settingsPath = join(homedir(), ".claude", "settings.json");
      const r = installSettings(settingsPath, "kt");
      process.stdout.write(r.changed ? `✓ đã cài vào ${settingsPath}${r.backup ? ` (backup: ${r.backup})` : ""}\n` : "✓ đã cài sẵn, không đổi gì\n");
      return;
    }
```

- [ ] **Step 5: Chạy test (xanh)**

Run: `bun test test/init.test.ts`
Expected: PASS 3 tests.

- [ ] **Step 6: Commit**

```bash
git add src/init.ts test/init.test.ts src/cli.ts
git commit -m "feat: kt init + doctor (cài hook/statusline idempotent)"
```

---

## Task 16: skill + README + smoke toàn bộ

**Files:**
- Create: `skills/concise-output.md`, `README.md`, `kt.json`

**Interfaces:**
- Consumes: toàn bộ.
- Produces: tài liệu + config mặc định.

- [ ] **Step 1: Tạo `kt.json`** (config mặc định để tham chiếu/override)

```json
{
  "profiles": { "test": true, "build": true, "install": true, "git": true, "generic": true },
  "generic": { "thresholdLines": 40, "headLines": 15, "tailLines": 10 },
  "git": { "maxHunkLinesPerFile": 20 },
  "store": { "keepRuns": 50 },
  "statusline": { "warnPct": 60, "dangerPct": 85 },
  "guard": { "maxCatKb": 100, "rules": { "findRoot": true, "npmLs": true, "treeNoDepth": true, "catBig": true } }
}
```

- [ ] **Step 2: Tạo `skills/concise-output.md`**

```markdown
---
name: concise-output
description: Bỏ preamble/postamble thừa khi trả lời, vẫn giữ tiếng người đầy đủ và dễ đọc. Dùng cho phiên pair-programming để giảm output token mà không giảm chất lượng.
---

# Concise Output

Khi trả lời trong phiên coding:

- BỎ: "Let me…", "I'll now…", "Great question!", lặp lại đề bài, tóm tắt lại thứ vừa làm nếu không được hỏi.
- GIỮ: kết luận, lệnh/code cần chạy, cảnh báo, lý do của quyết định quan trọng.
- Khi sửa code: nói NGẮN cái gì đổi + tại sao, không kể lại từng dòng.
- KHÔNG đánh đổi tính chính xác hay tính dễ đọc để ngắn. Đây KHÔNG phải "caveman" — vẫn câu cú đầy đủ.
```

- [ ] **Step 3: Tạo `README.md`**

```markdown
# kuro-lean (`kt`)

CLI giảm token Claude Code: nén output lệnh shell, đo context, chặn lệnh ngốn token.
Nguyên tắc: **nén nhiễu, giữ tín hiệu** — test fail/error luôn in đầy đủ.

## Cài
\`\`\`bash
bun link            # cho kt vào PATH
kt init             # đăng ký hook + statusline vào ~/.claude/settings.json
kt doctor           # kiểm tra
\`\`\`

## Subcommand
- \`kt run -- <cmd>\`  chạy lệnh, in bản nén, lưu full
- \`kt show [id]\`     xem full log
- \`kt status\`        statusline (đọc JSON stdin)
- \`kt init\` / \`kt doctor\`

## Bypass
- \`KT_RAW=1 kt run -- <cmd>\` hoặc xem \`kt show\`.

## Kết quả đo (điền sau smoke)
| Lệnh | Trước | Sau | Tiết kiệm |
|------|-------|-----|-----------|
| ...  | ...   | ... | ...       |
```

- [ ] **Step 4: Chạy toàn bộ test**

Run: `bun test`
Expected: PASS tất cả (generic, detect, runner, store, test/build/install/git compressors, config, registry, statusline, pipeline, hook-compress, hook-guard, init, smoke).

- [ ] **Step 5: Smoke end-to-end trong 1 phiên Claude Code thật**

- `bun link` rồi `kt init`, mở phiên Claude Code mới trong 1 repo có test.
- Chạy `npm test`/`git diff` → xác nhận output bị nén, statusline hiện %.
- Ghi 2-3 dòng số đo (token trước/sau) vào bảng README.

- [ ] **Step 6: Commit**

```bash
git add skills/concise-output.md README.md kt.json
git commit -m "docs: skill concise-output + README + config mặc định"
```

---

## Self-Review (đã chạy khi viết plan)

- **Spec coverage:** nén output (Task 6-9 + pipeline 11) ✓ · statusline (Task 11) ✓ · guard hook (Task 13) ✓ · skill concise (Task 16) ✓ · bypass/full log (Task 5, 11, 14) ✓ · exit code bảo toàn (Task 4, 11) ✓ · fallback raw (Task 10) ✓ · kt init/doctor (Task 15) ✓ · config (Task 10) ✓.
- **Placeholder scan:** không có TBD/TODO; mọi step code đầy đủ.
- **Type consistency:** `CompressInput/CompressResult/Compressor` (Task 2) dùng nhất quán mọi compressor; `Profile` (Task 3) dùng ở config/registry/hook; `Config` (Task 10) dùng ở pipeline/cli; `runAndCompress`/`renderStatusline` (Task 11) dùng ở cli; `decideCompress`/`decideGuard` (Task 12,13) dùng ở cli; `installSettings`/`runDoctor` (Task 15) dùng ở cli.
- **Rủi ro đã xử lý:** PreToolUse rewrite + statusline `context_window` đã verify với Claude Code docs (qua claude-code-guide).
